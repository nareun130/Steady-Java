# 대원칙
Java는 항상  변수의 값을 복사해서 대입한다.
```aiignore
기본형 -> 변수에 들어 있는 실제 사용값을 복사해서 대입
참조형 -> 변수에 들어 있는 참조값을 복사해서 대입
```

메서드 호출도 마찬가지.
 메서드 호출에 사용하는 파라미터도 결국 변수.</br>
-> 메서드 호출 시 매개변수에 값을 전달하는 것도 값을 복사해서 전달!!

# 절차 지향 프로그래밍
- 어떻게를 중심
- 실행 순서를 중요하게 -> 순차적
- 
# 객체 지향 프로그래밍
- 무엇을 중심
- 객체를 중요하게 -> 상호작용