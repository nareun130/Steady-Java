# 메서드 추출
- 중복제거
- 변경 영향 범위
- 메서드 이름 추가

=> 모듈화 : 메서드 호출만으로 손쉽게 사용 가능
```
지금까지는 절차지향 -> data와 기능이 분리되어 있기에

MusicPlayerData의 data가 변경 되면 MusicPlayerMain2의 메서드들도 함꼐 변경되어야 함.
-> 유지보수 관점에서 관리 포인트가 2곳으로 늘어남.
```

# 객체 지향 프로그래밍 
```aiignore
프로그래밍 실행 순서 보다 음악 플레이어 클래스 만드는 것 자체에 집중
-> 어떤 속성(데이터)를 가지고 어떤 기능(메서드)을 제공하는 지 이부분에 초점

※ 객체지향이든 절차지향이든 프로그래밍 실행순서는 둘다 중요. 무엇을 좀 더 중시하냐의 차이 
```

## 캡슐화
- 속성과 기능이 하나의 캡슐에 쌓여있는 것과 같다.
- MusicPlayer내부가 변하더라도 다른코드는 변경할 필요 x
- 물론 외부 메서드명 변경 시에는 변경해야 한다느 것

