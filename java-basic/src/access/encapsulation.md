# 캡슐화
-> 접근 제어라를 통해
## 1. 데이터를 숨겨라
- 객체 내부의 데이터를 외붕서 함부로 접근하지 못하게 해야 한다.
- 객체의 데이터 -> 객체의 메서드를 통해서 접근

## 2. 기능을 숨겨라
- 꼭 필요한 기능만 외부에 노출, 나머지 기능은 내부로 숨긴다.

```aiignore
BackAccount의 isAmountValid가 public일 경우에 BacknAacount를 사용하는 개발자 입장에서는 
이걸 사용해야 하는지 헷갈릴 수가 있음.
```