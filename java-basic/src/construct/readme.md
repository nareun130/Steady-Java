# this
```aiignore
생성자에서는 매개변수와 멤버변수의 이름이 같을경우, 매개변수를 더 우선시 함.
- 멤버변수를 가리킬때는 this를 쓴다.
-> this를 생략할 시, 둘 다 매개변수를 뜻하므로 멤버변수의 값이 바뀌지 않음.
```

# 생성자의 장점
- 중복 호출 제거
- 제약 : 생성자 호출 필수


=> 생성자를 사용하면 필수값 입력을 보장할 수 있다.
```aiignore
좋은 프로그램은 무한한 자유도가 주어지는 것이 아닌, 적절한 제약이 존재하는 프로그램
```